"""Cached, validated Client-Ready language.

Cached content is the required demonstration path: the workbench must work
with no network and no model credentials. Any optional live adapter has to
clear the same validator before its output is used.
"""
