# Make uncertainty and AI grounding visible

Client Cases will display Critical, High, or Watch urgency alongside a numeric score and visible factor breakdown. When sources conflict, the system will identify the conflict, reduce confidence, and withhold unsupported conclusions instead of silently choosing a value. AI explanations will be generated from a bounded Evidence Packet with structured output and claim-level source references; cached valid output may be used as a presentation fallback.
